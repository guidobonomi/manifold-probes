{
  "name": "manifold-block-probe",
  "version": "0.3.0",
  "description": "Probe: which JSON shape actually blocks a prompt? Two markers select two output shapes on UserPromptSubmit, so one session settles it. PreToolUse deny is already confirmed and kept as a control.",
  "author": {
    "name": "Manifold Security"
  }
}
