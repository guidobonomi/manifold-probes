{
  "name": "manifold-env-probe",
  "version": "0.2.0",
  "description": "Probe: what happens when configuration keys are renamed while an older plugin is still installed? v0.2.0 reads the renamed keys and logs both name sets plus its own version. Logs only.",
  "author": {
    "name": "Manifold Security"
  },
  "userConfig": {
    "tenant_ref": {
      "type": "string",
      "title": "Tenant ref",
      "description": "Renamed from tenant_id in v0.2.0. Logged in full.",
      "sensitive": false
    },
    "install_key": {
      "type": "string",
      "title": "Install key",
      "description": "Renamed from enrolment_key in v0.2.0. Only its length is logged.",
      "sensitive": true
    }
  }
}
